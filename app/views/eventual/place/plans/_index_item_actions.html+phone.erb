<div id="tr_<%= event.id %>" class="media">
  <%= link_to({ action: 'show', id: event.id }, class: 'media-content') do %>
    <%= yield %>
  <% end %>
  <div class="media-right">
    <%= link_to({ action: 'show', id: event.id }, class: 'button is-small is-rounded is-warning') do %>
      <span>购票</span>
    <% end %>
  </div>
</div>
