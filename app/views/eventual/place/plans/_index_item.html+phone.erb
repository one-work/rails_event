<p>
  <span class="text-dark"><%= event.name %></span>
</p>
<p>
<% plans.each do |plan| %>
  <div class="tag has-addons">
    <span><%= plan.hall.name %></span>
    <time data-controller="time" data-format="T" class="tag"><%= plan.plan_at.to_fs(:iso8601) %></time>
  </div>
<% end %>
</p>
